name: Keep Supabase Alive

on:
  schedule:
    # Har 3 din par dopahar 12 baje trigger hoga
    - cron: '0 12 */3 * *'
  workflow_dispatch: # Manually bhi run kar sakte hain

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase REST API
        run: |
          curl -s -X GET "https://cpvfatfjkpfdtoekqswu.supabase.co/rest/v1/categories?select=id&limit=1" \
            -H "apikey: sb_publishable_OjEqixPF5P8_u5pYdDljcg_D2poXAUY" \
            -H "Authorization: Bearer sb_publishable_OjEqixPF5P8_u5pYdDljcg_D2poXAUY"
